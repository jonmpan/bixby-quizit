action (CheckAnswerVoice) {
  type(Search)
  description (__DESCRIPTION__)
  collect {
    input (fullQuestion) {
      type (FullQuestion)
      min (Optional) max (One)
    }
    input (userAnswer){
      type(UserAnswer)
      min (Required) max (One)
    }
  }
  output (AnswerAndQuestion)
}
