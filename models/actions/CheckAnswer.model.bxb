action (CheckAnswer) {
  type(Search)
  description (__DESCRIPTION__)
  collect {
    input (quiz) {
      type (Quiz)
      min (Required) max (One)
    }
    input (userAnswer) {
      type (UserAnswer)
      min (Required) max (One)
    }
  }
  output (Quiz)
}
